# Local article data — must never end up in the image (PII, project state).
pipeline.db
pipeline.db-journal
pipeline.db-wal
pipeline.db-shm
articles.csv
articles.json
temp/

# Python caches and bytecode
__pycache__/
*.pyc
*.pyo
*.pyd

# IDE / editor
.vscode/
.idea/
*.swp

# Local env / credential files (defensive — see ../user_config.yaml)
.env
env.yaml
*.token.json

# VCS metadata (defensive — repo is not currently git-tracked)
.git/
.gitignore
